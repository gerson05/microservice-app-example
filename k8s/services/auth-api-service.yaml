apiVersion: v1
kind: Service
metadata:
  name: auth-api-service
  namespace: microservices
  labels:
    app: auth-api
spec:
  selector:
    app: auth-api
  ports:
  - port: 8081
    targetPort: 8081
    protocol: TCP
  type: ClusterIP
